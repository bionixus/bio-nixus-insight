# Proposal HTML archives

| File | Purpose |
|------|---------|
| `clinical-diagnostics-proposal-2026-source-copy.html` | Verbatim copy of the master proposal deck (local source). **Not served on the website.** Regenerate with `npm run build:clinical-proposal`. |
| `../public/conf/clinical-diagnostics-market-assessment-proposal.html` | Public redacted deck (no client names, no pricing). Served at `/clinical-diagnostics-market-assessment-proposal`. |

The master file under `Documents/01_BioNixus/...` is never modified by the build script.
