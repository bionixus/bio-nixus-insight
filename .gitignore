# Environment variables (secrets)
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local backups
bio-nixus-insight-website-backup-*.zip
.vercel
.env*.local

# Local scratch / debug output (not for repo)
tmp/
.xlsx-venv/

# Firecrawl CLI scrape/search outputs
.firecrawl/

# Google Search Console local CSV exports
.gsc-tmp/

# SEO regression suite — generated locally on every run, environment-specific
# (crawled host/URLs differ between local/preview/production); the committed
# artifact is reports/dedup-initial.json, not these.
/seo-report.json
/seo-baseline.json
/.seo-canary-state.json