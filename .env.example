# Statsig (feature flags, experiments, analytics)
VITE_STATSIG_CLIENT_KEY=client-xxx

# Sanity Blog – copy to .env and restart dev server
# CORS origins configured: http://localhost:8082 and https://bionixus.com
VITE_SANITY_PROJECT_ID=your-project-id
VITE_SANITY_DATASET=production
VITE_SANITY_API_TOKEN=sk...

# Sanity Case Studies
VITE_SANITY_CASE_STUDIES_PROJECT_ID=your-case-studies-project-id
VITE_SANITY_CASE_STUDIES_DATASET=production

# Sanity Server-side (write access for API routes — Editor role)
# Create at sanity.io/manage → API → Tokens
SANITY_API_TOKEN=sk...

# Resend (email sending)
# Get from resend.com → API Keys
RESEND_API_KEY=re_...
# Get from resend.com → Webhooks → signing secret
RESEND_WEBHOOK_SECRET=whsec_...

# AWS SES (optional — enables high-volume sending, overrides Resend)
# Set all 3 to enable SES. Get from AWS IAM → Create User with SES permissions
AWS_SES_ACCESS_KEY_ID=AKIA...
AWS_SES_SECRET_ACCESS_KEY=secret...
AWS_SES_REGION=eu-west-1
# From email (must be verified in SES)
AWS_SES_FROM_EMAIL=BioNixus Market Research <newsletter@bionixus.com>
# Reply-to address
AWS_SES_REPLY_TO=digital@bionixus.uk
# Optional: SES Configuration Set name (for open/click tracking via SNS)
AWS_SES_CONFIG_SET=

# App
VITE_BASE_URL=https://bionixus.com

# Admin (choose a strong password)
ADMIN_PASSWORD=change-me
